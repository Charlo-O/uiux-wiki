# UI 条目逐项审计账本

规则：只记录已经逐一核对并落到代码的条目。没有单独检查、单独修改、单独验证的条目，不写入“已完成”。

| 顺序 | 条目 ID | 一级 / 二级 | 审计结果 | 代码入口 | 验证 |
| ---: | --- | --- | --- | --- | --- |
| 1 | `components-text` | 组件 / Components > 基础元素 / Primitives | 已按 Text 定义重写详情字段、独立预览、缩略预览、截断/选中/辅助状态；不再复用 `typography-block` | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动验证通过；唯一 overflow 为刻意展示的 `.audited-text-truncate` |
| 2 | `components-heading` | 组件 / Components > 基础元素 / Primitives | 已按 Heading 定义重写详情字段、独立预览、缩略预览、H1/H2/H3 层级、锚点按钮和长标题状态；不再复用 `typography-block` | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动验证通过；锚点按钮有 aria-label、`type=button`、40x40 尺寸 |
| 3 | `components-subtitle` | 组件 / Components > 基础元素 / Primitives | 已按 Subtitle 定义重写详情字段、独立预览、缩略预览、标题支撑说明、范围说明和弱化/强调状态；不再复用 `typography-block` | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Playwright 桌面/390px 移动验证通过；无旧模板、无横向溢出、无 console/page error |
| 4 | `components-paragraph` | 组件 / Components > 基础元素 / Primitives | 已按 Paragraph 定义重写详情字段、独立预览、缩略预览、连续段落、行宽/行高/段距和选中/折叠状态；从错误的 `chart-panel` 改为段落阅读预览 | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Playwright 桌面/390px 移动验证通过；旧 `chart-panel`/`typography-block` 均消失，无横向溢出、无 console/page error |
| 5 | `components-caption` | 组件 / Components > 基础元素 / Primitives | 已按 Caption 定义重写详情字段、独立预览、缩略预览、figure/figcaption、来源链接和弱化/错误说明状态；不再复用 `typography-block` | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Playwright 桌面/390px 移动验证通过；来源链接 40px 高，figure/figcaption 存在，无旧模板、无横向溢出、无 console/page error |
| 6 | `components-helper-text` | 组件 / Components > 基础元素 / Primitives | 已按 Helper Text 定义重写详情字段、独立预览、缩略预览、字段关联、aria-describedby、错误/成功/字数提示；不再复用 `support-panel` | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动验证通过；`aria-describedby` 关联 3 条 helper line，`aria-invalid=true`，无旧模板、无横向溢出、无 console error |
| 7 | `components-description` | 组件 / Components > 基础元素 / Primitives | 已按 Description 定义重写详情字段、独立预览、缩略预览、对象摘要、元信息和展开入口；不再复用 `typography-block` | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动验证通过；展开按钮 `type=button` 且 40px 高，无旧模板、无横向溢出、无 app console error |
| 8 | `components-label` | 组件 / Components > 基础元素 / Primitives | 已按 Label 定义重写详情字段、独立预览、缩略预览、label/htmlFor 绑定、inline/top label、必填附属标记；不再复用 `typography-block` | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动验证通过；点击 Label 可聚焦输入框，input/select 绑定存在，无旧模板、无横向溢出、无 app console error |
| 9 | `components-required-mark` | 组件 / Components > 基础元素 / Primitives | 已按 Required Mark 定义重写详情字段、独立预览、缩略预览、星号/必填文字/错误说明/通过状态；视觉标记不再单独承担语义，不再复用 `typography-block` | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`required`、`aria-required=true`、`aria-invalid=true`、`aria-describedby=required-preview-error` 一致，点击 Label 可聚焦字段，stage 包住预览且 PreviewContext 不重叠；Chrome 截图证据已生成 |
| 10 | `components-optional-mark` | 组件 / Components > 基础元素 / Primitives | 已按 Optional Mark 定义重写详情字段、独立预览、缩略预览、可选胶囊、稍后按钮、跳过/填写增强状态；不再复用 `typography-block`，也不套用必填标记的红色错误样式 | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；字段无 `required`，`aria-required=false`、`aria-describedby=optional-preview-hint`，点击 Label 可聚焦字段，`稍后` 按钮 `type=button` 且高度合格，stage 包住预览且 PreviewContext 不重叠；Chrome 截图证据已生成 |
| 11 | `link` | 组件 / Components > 基础元素 / Primitives | 已按 Link 定义重写详情字段、独立预览、缩略预览、正文/访问过/焦点/禁用链接状态；使用 anchor/href，不再复用旧 `live-link-card` 泛预览 | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；详情中 3 个链接均为 `<a href>` 且高度合格，无 button，禁用项为 `span[aria-disabled=true]` 且无 href，焦点链接可聚焦，stage 包住预览且 PreviewContext 不重叠；Chrome 截图证据已生成 |
| 12 | `components-external-link` | 组件 / Components > 基础元素 / Primitives | 已按 External Link 定义重写详情字段、独立预览、缩略预览、外链图标、域名提示、新窗口/安全 rel、禁用外链状态；不再复用普通 `link` 预览 | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；详情中 2 个外链均为 `<a href>` 且 `target="_blank"`、`rel="noreferrer"`、高度合格，外链图标 `aria-hidden=true`，禁用项为 `span[aria-disabled=true]` 且无 href，焦点链接可聚焦，stage 包住预览且 PreviewContext 不重叠；Browser 截图超时后用 Chrome 生成截图证据 |
| 13 | `components-icon` | 组件 / Components > 基础元素 / Primitives | 已按 Icon 定义重写详情字段、独立预览、缩略预览、命名图标、语义图标、错误状态图标、装饰隐藏图标和尺寸/描边说明；不再复用 `icon-signal`，也不误做成 Icon Button | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-icon` 使用 `.mini-audited-icon.semantics`，第 14 项 Decorative Icon 仍未标完成；详情中 4 个图标样本均为 24px，2 个语义样本有 `role=img` 与 `aria-label`，装饰样本 SVG 为 `aria-hidden=true`、`focusable=false`，无 button/link 误用；修复侧栏详情横向 overflow，Browser 截图超时后用 Chrome 生成截图证据 |
| 14 | `components-decorative-icon` | 组件 / Components > 基础元素 / Primitives | 已按 Decorative Icon 定义重写详情字段、独立预览、缩略预览、装饰 SVG、文本承载真实含义、读屏隐藏和无交互状态；不再复用 `icon-signal`，也不误算为 Status Icon 或 Icon Button | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-decorative-icon` 使用 `.mini-audited-decorative-icon.hidden`，第 15 项 Status Icon 仍未标完成；详情中 6 个 SVG 均为 `aria-hidden=true`、`focusable=false`，无 `role=img`、无 aria 名称、无 tabindex、无 button/link；桌面与移动均无横向 overflow，Browser 截图超时后用 Chrome 生成截图证据 |
| 15 | `components-status-icon` | 组件 / Components > 基础元素 / Primitives | 已按 Status Icon 定义重写详情字段、独立预览、缩略预览、成功/警告/错误/进行中四种状态、状态文字和 aria 状态；不再复用 `icon-signal`，也不误算为 Decorative Icon、Status Dot 或 Status Badge | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-status-icon` 使用 `.mini-audited-status-icon.state`，第 16 项 Brand Icon 仍未标完成；详情中 4 个状态行均为 `role=status` 且有 `aria-label` 和可见文字，SVG 均 `aria-hidden=true` 且无 aria 名称，无 button/link/tabindex；桌面与移动均无横向 overflow，Browser 截图超时后用 Chrome 生成截图证据 |
| 16 | `components-brand-icon` | 组件 / Components > 基础元素 / Primitives | 已按 Brand Icon 定义重写详情字段、独立预览、缩略预览、品牌锁定、主色/单色/反白/应用图标版本、固定比例和安全区说明；不再复用 `icon-signal`，也不误算为 Decorative Icon、Status Icon 或 Icon Button | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-brand-icon` 使用 `.mini-audited-brand-icon.identity`，第 17 项 Badge 仍未标完成；详情中 5 个品牌标志保持方形比例，锁定字标内图标 `aria-hidden=true`，4 个独立版本均为 `role=img` 且有 aria-label，无 button/link/tabindex；修复侧栏品牌版本被压成非方形的问题，桌面与移动均无横向 overflow，Browser 截图超时后用 Chrome 生成截图证据 |
| 17 | `badge` | 组件 / Components > 基础元素 / Primitives | 已按 Badge 定义重写详情字段、独立预览、缩略预览、New/Beta/Pro/Lab 短文本徽标、非交互和元信息说明；不再复用旧 `badge` 数字角标或 `live-social-card`，也不误算为 Count Badge、Status Badge 或 Tag | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`badge` 使用 `.mini-audited-badge.label`，第 18 项 Count Badge 仍未标完成；详情中 4 个徽标均为短文本且无纯数字，Beta 带 `aria-label=Beta 功能`，无 `role=status`、无 button/link/tabindex；桌面与移动均无横向 overflow，Browser 截图超时后用 Chrome 生成截图证据 |
| 18 | `components-count-badge` | 组件 / Components > 基础元素 / Primitives | 已按 Count Badge 定义重写详情字段、独立预览、缩略预览、未读/上限/紧凑/0 隐藏数量场景、完整数量 aria 和非交互规则；不再复用普通 `badge`，也不误算为 Badge、Status Dot 或 Status Badge | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-count-badge` 使用 `.mini-audited-count-badge.counter`，第 19 项 Status Dot 仍未标完成；详情中 4 个数量目标包含 `12`、`99+`、`3` 和 0 隐藏，`99+` aria-label 保留完整 132 条，0 值 badge `aria-hidden=true` 且视觉隐藏，无 button/link/tabindex/role=status；桌面与移动局部容器均无横向 overflow，Browser 截图超时后用 Chrome 生成截图证据 |
| 19 | `components-status-dot` | 组件 / Components > 基础元素 / Primitives | 已按 Status Dot 定义重写详情字段、独立预览、缩略预览、在线/忙碌/同步中/离线点状状态、外层状态语义、减少动效规则和非交互边界；不再复用 `icon-signal`，也不误算为 Status Icon、Status Badge 或 Count Badge | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-status-dot` 使用 `.mini-audited-status-dot.presence`，第 20 项 Status Badge 仍未标完成；详情中 4 个状态行均为 `role=status` 且有完整 `aria-label` 和可见文字，4 个 `.audited-status-dot` 均 `aria-hidden=true`、无 aria-label、无 tabindex，点尺寸 12x12，无 button/link；桌面与移动局部容器均无横向 overflow，Browser 截图超时后用 Chrome 生成截图证据 |
| 20 | `components-status-badge` | 组件 / Components > 基础元素 / Primitives | 已按 Status Badge 定义重写详情字段、独立预览、缩略预览、已通过/待确认/失败/同步中四种带文字状态胶囊、完整 aria 状态和非交互边界；不再复用普通 `badge`，也不误算为 Status Dot、Status Icon、Count Badge 或 Tag | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-status-badge` 使用 `.mini-audited-status-badge.feedback`，第 21 项 Tag 仍未标完成；详情中 4 个 `.audited-status-badge` 均为 `role=status` 且有完整 `aria-label` 和可见文字，SVG 图标均 `aria-hidden=true`，无 button/link/input/tabindex/关闭控件，不含纯数字数量或 New/Beta/Pro 普通 Badge 文案；桌面与移动局部容器均无横向 overflow，Browser 截图超时后用 Chrome 生成截图证据 |
| 21 | `tag` | 组件 / Components > 基础元素 / Primitives | 已按 Tag 定义重写详情字段、独立预览、缩略预览、静态分类标签、可筛选标签、可移除标签和禁用标签；不再复用旧 `tag`/`live-social-card`，也不误算为 Badge、Status Badge、Count Badge 或 Chip | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`tag` 使用 `.mini-audited-tag.taxonomy`，第 22 项 Chip 仍未标完成；详情中 7 个 `.audited-tag` 覆盖静态/筛选/移除/禁用，筛选按钮均为 `type=button` 且可真实切换 `aria-pressed`，移除按钮均为 `type=button` 且有 `aria-label` 和 `aria-hidden=true` 的关闭图标，静态标签无 button/link/tabindex，禁用标签 `aria-disabled=true`，无 `role=status`、无纯数字数量、无 New/Beta/Pro 普通 Badge 文案；移动端所有 Tag 按钮触控尺寸 44px，桌面与移动局部容器均无横向 overflow，Browser DOM 验证后用 Chrome 生成截图证据 |
| 22 | `components-chip` | 组件 / Components > 基础元素 / Primitives | 已按 Chip 定义重写详情字段、独立预览、缩略预览、对象 token、选择 Chip、可移除 Chip、禁用 Chip 和输入框 token 场景；不再复用 `badge`，也不误算为 Tag、Badge、Status Badge 或 Count Badge | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-chip` 使用 `.mini-audited-chip.token`，第 23 项 Close Button 仍未标完成；详情中 5 个 `.audited-chip` 覆盖 token/选择/禁用，选择 Chip 均为 `type=button` 且可真实切换 `aria-pressed`，可移除 token 的关闭按钮均为 `type=button` 且有 `aria-label` 和 `aria-hidden=true` 图标，头像 `aria-hidden=true`，输入框有 `label[for=audited-chip-input]` 与 `aria-label=添加成员 Chip`，禁用 Chip `aria-disabled=true`，无 `role=status`、无纯数字数量、无 New/Beta/Pro 普通 Badge 文案；移动端 Chip 按钮与输入框触控尺寸 44px，桌面与移动局部容器均无横向 overflow，Browser DOM 验证后用 Chrome 生成截图证据 |
| 23 | `components-close-button` | 组件 / Components > 基础元素 / Primitives | 已按 Close Button 定义重写详情字段、独立预览、缩略预览、面板关闭、Toast/提示关闭、内联说明关闭、禁用关闭按钮和真实 dismiss 交互；不再复用通用 `button`，也不误算为 More、Back、Clear、Delete 或 Cancel | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-close-button` 使用 `.mini-audited-close-button.dismiss`，第 24 项 More Button 仍未标完成；详情中 4 个 `.audited-close-button` 全部为 `type=button`，3 个可用、1 个 `disabled`，aria-label 分别为“关闭设置面板 / 关闭同步提示 / 关闭权限说明 / 关闭已锁定提示”，所有 X 图标 `aria-hidden=true` 且 `focusable=false`，无通用按钮模板、无 link/input/role=status、无删除/返回/清空/更多/取消/确定误语义；修复了内部 `.toast` 类名撞全局 Toast 样式导致按钮隐藏的问题；点击“关闭同步提示”后仅移除 `toast` 目标，panel/inline/locked 保持；移动端 4 个按钮均为 44x44，详情容器无横向 overflow；Browser 截图超时后用 Chrome/CDP 生成桌面与移动截图证据 |
| 24 | `components-more-button` | 组件 / Components > 基础元素 / Primitives | 已按 More Button 定义重写详情字段、独立预览、缩略预览、overflow trigger、展开菜单、菜单项、禁用更多按钮和 Escape 收起交互；不再复用通用 `button`，也不误算为 Close、Back、Clear、Load More、Cancel 或具体动作按钮 | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-more-button` 使用 `.mini-audited-more-button.overflow`，第 25 项 Help Button 仍未标完成；详情中主触发器为 `type=button`，有 `aria-haspopup=menu`、`aria-expanded`、`aria-controls=audited-more-menu` 和目标明确的 `aria-label=打开设计稿资产更多操作`，禁用触发器为 `disabled` 且 `aria-expanded=false`；点击触发器后菜单真实进入 DOM，`role=menu`、4 个 `role=menuitem` 均为 `type=button`，菜单项选择后收起菜单并更新反馈，Escape 可收起；所有省略号和菜单图标 `aria-hidden=true` 且 `focusable=false`，无通用按钮模板、无 Close/Clear/Back/Load More 误语义；移动端触发器、禁用按钮和菜单项均为 44px 高，详情容器与菜单无横向 overflow；Browser 截图超时后用 Chrome/CDP 生成桌面与移动展开态截图证据 |
| 25 | `components-help-button` | 组件 / Components > 基础元素 / Primitives | 已按 Help Button 定义重写详情字段、独立预览、缩略预览、上下文帮助触发器、帮助面板、禁用帮助按钮和 Escape 收起交互；不再复用通用 `button` 或 `support-panel`，也不误算为 Info Button、More Button、Close Button 或客服入口 | `src/data.js`, `src/App.jsx`, `src/styles.css` | Browser 代码断言通过；`components-help-button` 使用 `.mini-audited-help-button.guidance`，第 26 项 Info Button 仍未标完成且仍为通用 `button` 预览；详情中主触发器为 `type=button`，有 `aria-haspopup=dialog`、`aria-expanded`、`aria-controls=audited-help-panel` 和目标明确的 `aria-label=打开 API 密钥命名帮助`，禁用触发器为 `disabled` 且 `aria-expanded=false`；点击触发器后帮助面板真实进入 DOM，`role=dialog`、`aria-label=API 密钥命名帮助`、示例文本 `prod-billing-2026q2` 均存在，且无 `role=menu` / `role=menuitem`；Escape 只收起帮助面板并保留详情弹窗，`aria-expanded` 回到 `false`；所有帮助图标 `aria-hidden=true` 且 `focusable=false`；390px 应用内移动模式中触发器和禁用按钮均满足 44px 命中区域，帮助面板落在手机壳和 Help Button 卡片内，详情弹窗、stage、mobile surface、mobile screen、variant shell 与 Help Button 卡片均无横向 overflow |
| 26 | `components-info-button` | 组件 / Components > 基础元素 / Primitives | 已按 Info Button 定义重写详情字段、独立预览、缩略预览、静态信息触发器、信息气泡、禁用信息按钮和 Escape 收起交互；不再复用通用 `button`，也不误算为 Help Button、More Button、Close Button 或客服入口 | `src/data.js`, `src/App.jsx`, `src/styles.css` | Browser 代码断言通过；`components-info-button` 使用 `.mini-audited-info-button.facts`，第 27 项 Copy Button 仍未标完成且仍为通用 `button` 预览；详情中主触发器为 `type=button`，有 `aria-expanded`、`aria-controls=audited-info-tooltip`，打开时有 `aria-describedby=audited-info-tooltip-copy`，且没有 `aria-haspopup`；禁用触发器为 `disabled` 且 `aria-expanded=false`；点击触发器后信息气泡真实进入 DOM，`role=tooltip`、`aria-label=转化率口径信息`、静态事实文本“转化率 = 完成订单用户 / 访问结算页用户”“每 15 分钟刷新”“Analytics v3”均存在，且无 `role=menu` / `role=menuitem` / `role=dialog`，气泡内无 button/link/input；Escape 只收起信息气泡并保留详情弹窗，`aria-expanded` 回到 `false`；所有信息图标 `aria-hidden=true` 且 `focusable=false`；390px 应用内移动模式中触发器和禁用按钮均满足 44px 命中区域，信息气泡落在手机壳和 Info Button 卡片内，详情弹窗、stage、mobile surface、mobile screen、variant shell 与 Info Button 卡片均无横向 overflow |
| 27 | `components-copy-button` | 组件 / Components > 基础元素 / Primitives | 已按 Copy Button 定义重写详情字段、独立预览、缩略预览、剪贴板目标、复制成功/失败反馈、禁用复制按钮和移动端收窄布局；不再复用通用 `button`，也不误算为 Duplicate Button、Share Button、More Button、Info Button 或 Help Button | `src/data.js`, `src/App.jsx`, `src/styles.css` | Browser 代码断言通过；`components-copy-button` 使用 `.mini-audited-copy-button.clipboard`，第 28 项 Share Button 仍未标完成且仍为通用 `button` 预览；详情 modal 中使用 `.live-audited-copy-button-card.clipboard`，无通用 `.preview-button` / `.live-button-card` / `.live-semantic-card` / `.live-tool-card`，且无 More/Help/Info/Close 错模板；3 个 `.audited-copy-button` 均为 `type=button`，两个可用按钮分别绑定 `#audited-copy-source-invoice` 与 `#audited-copy-source-invite`，都有目标明确的 `aria-label` 与 `aria-describedby=复制源 + audited-copy-feedback`，禁用按钮为 `disabled` 且说明隐藏 Token 暂不可复制；所有 Copy/Check 图标 `aria-hidden=true` 且 `focusable=false`；卡片内部无 `aria-haspopup`、无 `aria-expanded`、无 `role=menu` / `role=menuitem` / `role=dialog`；点击发票号后剪贴板读到 `INV-2026-0842`，按钮变为“已复制”，`aria-label` 更新为“已复制发票号 INV-2026-0842，可再次复制”，`aria-live=polite` 反馈为“已复制发票号”；点击邀请链接后剪贴板读到 `https://kandong.app/invite/ui-27`，且 copied 状态只落在当前目标；390px viewport 中 dialog、stage、variant shell、Copy card、board、三张 target card、反馈和规则说明均无横向 overflow，按钮均满足 44px 命中区域；子代理 A/B/C 已并行只读复查旧标准、第 27 语义和第 28 预备状态 |
| 28 | `components-share-button` | 组件 / Components > 基础元素 / Primitives | 已按 Share Button 定义补齐数据语义、专属缩略预览、详情 live preview、分享目标面板、权限禁用态、成功/取消/fallback 反馈与移动端收窄布局；不再复用通用 `button`，也不误算为 Copy Button、More Button、Help Button、Info Button、Send Button 或 Social Share Button | `src/data.js`, `src/App.jsx`, `src/styles.css` | `npm run build` 通过；Browser 桌面/390px 移动逐项验证通过；`components-share-button` 使用 `.mini-audited-share-button.distribution` 与 `.live-audited-share-button-card.distribution`，第 29 项 `components-favorite-button` 仍未标完成且仍为 generic `button`；详情主触发器为 `type=button`，有 `aria-haspopup=dialog`、`aria-expanded`、`aria-controls=audited-share-panel`、`aria-describedby=audited-share-resource-copy audited-share-feedback` 和明确 `aria-label=分享设计系统套装`；打开后面板真实进入 DOM，`role=dialog` 且 `aria-label=分享设计系统套装`，包含 3 个 `share-channel` 按钮，均为 `type=button`、无 `role=menu/menuitem`，图标均 `aria-hidden=true` 且 `focusable=false`；分享到“设计系统团队”后面板关闭、`aria-expanded=false`、反馈为 `share-action-note success`；取消按钮 44x44，点击后反馈为 `share-action-note cancel`；复制邀请链接成功时写入 `https://kandong.app/share/design-system-kit` 并进入 fallback 成功态，若浏览器拒绝剪贴板则保留面板并显示 `.share-manual-link` 只读输入框供手动复制；私密草稿按钮为真实 `disabled` 且说明不可分享；无通用 `.preview-button` / `.live-button-card` / `.live-semantic-card` / `.live-tool-card`，无 Copy/More/Help/Info 错模板；Share 新增样式无 `linear-gradient`/`radial-gradient` 命中；390px 下 dialog、stage、variant shell、Share card、board、panel 均无横向 overflow，所有可用按钮命中区域均不小于 44px；子代理已并行只读确认指定 commit `f0f4e92` 的旧版标准和 Docs 第 28/29/30 项顺序 |
